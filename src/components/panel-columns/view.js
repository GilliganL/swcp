import React from 'react';
import PropTypes from 'prop-types';
import styles from './styles.module.scss';

const View = () => (
  <div>Boilerplate</div>
);
View.propTypes = {};
View.defaultProps = {};

export default View;
